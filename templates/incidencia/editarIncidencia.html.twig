{# empty Twig template #}
{%include 'menu.html.twig' %}
<link rel="stylesheet" href="/css/style.css">
<section class="form-register">
    <h1>Editar Incidencia</h1>
    <br>
    <form action="{{ path("editar_incidencia", {id:incidencia.id})}}" method="post">
        Título
        <input type="text" name="titulo" placeholder="titulo..." value="{{ incidencia.titulo }}" class="controls"><br>
        Estado
        <select name="estado" class="controls">
            <option value="iniciada" {% if incidencia.estado == "iniciada" %}selected{% endif %}>Iniciada</option>
            <option value="en proceso" {% if incidencia.estado == "en proceso" %}selected{% endif %}>En proceso</option>
            <option value="resuelta"{% if incidencia.estado == "resuelta" %}selected{% endif %}>Resuelta</option>
        </select><br>
        <input type="submit" class="botons"value="Modificar Incidencia">
    </form>
</section>    